
מטרה: לגרום ללשונית "תלמידי בית ספר מנגן" להיראות ולהתנהג כמו רשימת התלמידים הרגילה, עם RTL מלא ויישור לימין בכל הרכיבים — כולל מספר תלמיד, שורת הקטגוריות, הפילטרים והפרטים המורחבים.

שלבי יישום:
1. ליישר את מבנה הרשימה לפי `AdminStudents`
   - לעדכן את כרטיס הסיכום של כל תלמיד ב-`src/pages/admin/AdminSchoolMusicSchools.tsx` כך שישתמש באותו מבנה חזותי של רשימת התלמידים הרגילה:
     - מספר תלמיד במיקום הימני
     - שם תלמיד בולט
     - שורת קטגוריות טקסטואלית עם מפרידים במקום אימוג'ים
     - באדג'ים מיושרים כמו ברשימה הרגילה
   - לשמור על הלחיצה לפתיחת פרטים מורחבים, אבל לבנות את שורת הסיכום באותו סגנון של `AdminStudents`.

2. ליישר את אזור החיפוש והסינונים לימין
   - להחליף את גריד הפילטרים הנוכחי במבנה `flex-wrap` כמו ב-`AdminStudents`, כדי שכל הסלקטים והחיפוש יסתדרו טבעית ב-RTL.
   - ליישר גם את שורת הספירה (`X תלמידים`) לימין באופן עקבי.

3. לתקן את הפרטים המורחבים כך שיהיו RTL אמיתי
   - לעדכן את `DetailRow` ואת אזור ה-expanded כך שכל שורה תוצג במבנה קבוע של ימין-לשמאל, בלי "נפילה" ל-LTR.
   - לחלק את הפרטים לסקשנים ברורים בדומה לכרטיס תלמיד רגיל: פרטי תלמיד, פרטי הורה, פרטי כלי/שיוך.
   - להבטיח ששדות כמו טלפון/אימייל בלבד יישארו `dir="ltr"` כשצריך, בלי להפוך את כל הבלוק לשמאל-לימין.

4. ליישר גם את לשונית בתי הספר באותו קובץ
   - מאחר שהמבנה של אותו עמוד כבר הושפע בעבר, אחזק גם את רשימת בתי הספר כדי לוודא שהיא שומרת על RTL עקבי ולא נשברת בעקבות השינויים בלשונית התלמידים.

5. QA ממוקד
   - לבדוק ב-`/admin/school-music-schools`:
     - לשונית בתי ספר
     - לשונית תלמידים
     - פתיחת פרטים מורחבים
     - מצב עריכה
     - יישור מספר תלמיד, שורת הקטגוריות, באדג'ים, חיפוש ופילטרים
   - לבדוק גם בתצוגת מובייל כדי לוודא שהעטיפה והשבירה של השורות נשארות RTL.

פרטים טכניים:
- קובץ עיקרי לשינוי: `src/pages/admin/AdminSchoolMusicSchools.tsx`
- קבצי ייחוס לעיצוב/מבנה:
  - `src/pages/admin/AdminStudents.tsx`
  - `src/pages/admin/AdminStudentCard.tsx`
- לא נדרש שינוי בטבלאות או בבקאנד.
- לא אשנה את התנהגות הנתונים או הפילטרים עצמם, אלא בעיקר את מבנה ה-UI, סדר האלמנטים, ועטיפות RTL כדי שהמסך ייראה כמו מודול התלמידים הקיים ויישאר עקבי בכל רכיב חדש בתוך העמוד.
